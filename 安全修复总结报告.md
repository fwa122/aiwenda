# 安全专项修复总结报告

- **项目**：AI 知识库平台（Vue3 + NestJS + FastAI/Celery）
- **日期**：2026-09-07
- **范围**：内部接口鉴权、文件上传防护、跨域收紧、密钥管理、越权访问（IDOR）、路径穿越、前端渲染防护

---

## 一、修复总览

| # | 问题 | 严重度 | 状态 | 验证结果 |
|---|---|---|---|---|
| 1 | AI 服务内部接口完全无鉴权 | 严重 | ✅ 已修复 | 无/错 token → 401；正确 token → 200 |
| 2 | 越权访问（IDOR）普遍缺失 | 严重 | ✅ 已修复 | 非 owner 全部 403；private 隔离生效 |
| 3 | 文件上传无白名单/大小限制 | 严重 | ✅ 已修复 | .exe → 400；21MB → 413；合法文件正常 |
| 4 | CORS 全开 | 中等 | ✅ 已修复 | 白名单放行；恶意 origin 拒绝 |
| 5 | JWT 弱默认密钥、无启动校验 | 中等 | ✅ 已修复 | 弱密钥拒绝启动；secret 已轮换 |
| 6 | 路径拼接未校验 `..`（ai-service） | 边缘 | ✅ 已修复 | `../../` 路径被拒并永久失败 |
| 7 | DOMPurify 允许 `target` 未配 `rel` | 边缘 | ✅ 已修复 | 钩子强制补 `rel="noopener noreferrer"` |

---

## 二、详细修复内容

### 批 1：防御加固

#### 1. 内部接口鉴权（fail-closed 设计）

- **ai-service**（`app/api/internal.py`）：新增 `verify_internal` 依赖，挂在 `/internal` 路由组上——校验 `Authorization: Bearer`，使用 `secrets.compare_digest` 常量时间比较防时序攻击；`/internal/health` 豁免（探活用）
- **关键设计**：`INTERNAL_TOKEN` 未配置时，受保护端点一律 503 而非放行——部署遗漏配置的表现是"功能不可用"（立即发现），而非"静默裸奔"（无人知晓）
- **server**（`src/integrations/ai-service.client.ts`）：新增 `authHeaders()` 统一注入令牌，覆盖 parse 投递、search、chat SSE 三处出口
- **令牌**：`openssl rand -hex 32` 生成，写入 `ai-service/.env`（INTERNAL_TOKEN）与 `server/.env`（AI_SERVICE_TOKEN）

#### 2. 上传白名单（三道闸）

- 扩展名白名单 `pdf / docx / txt / md / csv`（`src/document/document.service.ts`，与 ai-service `parser.py` 支持严格对齐）
- 扩展名格式校验 `^[a-z0-9]{1,10}$`（兜住路径注入与怪字符；原实现 `split('.').pop()` 可携带 `/` 等字符）
- Multer `limits.fileSize = 20MB`（`document.controller.ts`，超限自动映射 413）

#### 3. CORS 白名单

- `src/main.ts`：origin 白名单（`FRONTEND_URL` 环境变量，逗号分隔多域名，默认含 5173/4173 本地开发地址）；`credentials: false`（前端走 Authorization 头，不依赖 cookie）

#### 4. JWT 密钥 fail-fast

- `src/main.ts`：`assertStrongJwtSecret()` 启动时校验（≥32 字符且非默认值），不满足直接 `process.exit(1)` 并打印生成指引
- **JWT_SECRET 已轮换**（64 位 hex）——所有旧登录态失效，需重新登录

### 批 2：越权修复（IDOR）

#### 数据模型

- `KnowledgeBase` 新增 `userId` 外键（`onDelete: SetNull` + 索引），`User` 补反向关系
- **迁移方式**：Prisma `migrate dev` 检测到迁移历史与数据库 drift（此前 HNSW 索引曾被自动迁移误删）要求重置数据库，改用 `db push` 非破坏同步 + 手工回填 `owner → user_id`
- 存量数据回填结果：现有知识库归属 admin

#### 统一权限模型（`src/common/kb-access.ts`）

| 操作 | 规则 |
|---|---|
| 读（internal/public 库） | 所有登录用户 |
| 读（private 库） | 仅 owner 与 admin |
| 写（update/remove/reindex/上传/重解析） | 仅 owner 与 admin |
| 无主库（user_id 为 NULL 的遗留数据） | 仅 admin 可写 |

#### 覆盖点位

- **kb.service**：list/options 按可见性过滤（`AND` 组装，避免与 keyword 的 `OR` 冲突）；detail 读校验；create 写入归属；update/remove/reindex 走 `assertWritable`
- **kb.controller**：retrieval-test 校验指定 kbId 的可见性（防枚举他人 private 库）
- **document.service**：`assertKb(kbId, user, 'read'|'write')`——文档作为子资源，权限完全由父知识库决定；upload/list/remove/reparse/chunks 全覆盖
- **chat.service**：新增 `filterVisibleKbs()`，对话/检索前过滤 kbIds（防直接传参绕过 private 限制）
- **api-key.service**：`list()` 按 userId 过滤（原返回全库密钥）；`remove()` 归属校验（原仅查存在性）
- **说明**：conversation 模块经核实本就有完整归属校验（assertOwned / 按 userId 过滤 / feedback 归属检查），未做改动

### 边缘项修复

#### 5. 路径穿越防护（`ai-service/app/tasks.py`）

- `storage_key` 拼接后 `abspath` 规范化，必须仍位于 `upload_root` 内（目录边界校验防前缀绕过），否则拒绝
- **端到端验证**：向库中插入 `storage_key='../../evil.md'` 的测试文档 → 投递解析 → worker 触发防护，文档标记 `failed：非法的文件路径: ../../evil.md`

#### 6. 反向标签劫持防护（`frontend/src/utils/markdown.js`）

- DOMPurify `afterSanitizeAttributes` 钩子：所有带 `target` 的链接强制补 `rel="noopener noreferrer"`

#### 7. 重试策略优化（随路径防护发现）

- 原 `except Exception` 无差别重试：确定性错误（非法路径、空切片）也会进入 60 秒指数退避空转
- 现区分：`ValueError`（确定性错误）→ 立即永久失败；其余（网络/限流类瞬时错误）→ 保留指数退避（Celery 默认最多 3 次）
- 复测确认：恶意路径任务无重试日志

---

## 三、过程中发现并修复的既有缺陷

| 缺陷 | 影响 | 修复 |
|---|---|---|
| kb.controller 的 `result.then(记日志)` 无 `.catch` | 校验失败（403/404）时产生 unhandled rejection，Node 22 默认行为下**直接崩溃整个进程**（验证阶段实测复现） | create/remove 补 `.catch(() => {})`，异常仍正常返回给客户端 |
| 同文件并行 Edit 竞争 | 实施中两次导致修改互相覆盖丢失（ai-service.client.ts、main.ts） | 已串行重做并逐次验证落盘 |
| `INTERNAL_TOKEN` 在 .env 中重复 | 新增行与旧的空值行共存 | 已去重（保留有值行） |

---

## 四、配置与交付物变更

| 变更 | 说明 |
|---|---|
| `INTERNAL_TOKEN` / `AI_SERVICE_TOKEN` | 新增（同一 hex32 令牌，两边 .env 均已被 gitignore） |
| `JWT_SECRET` | 轮换为 64 位 hex；**所有登录态失效** |
| `server/.env.example`、`ai-service/.env.example` | 新增脱敏配置模板（含各配置项用途注释） |
| `frontend/.env.development` | 无敏感信息，gitignore 中显式放行 |
| 测试账号 | 验证中注册 `demo / demo1234`（u_8d5f0b12e459），可留作演示或删除 |

---

## 五、遗留事项与建议

1. **前端体验**（可选）：非 owner 用户界面上仍显示知识库编辑/删除按钮，点击后才收到 403——可按当前用户隐藏操作按钮
2. **密钥管理**：今后若将仓库推送到远端，推送前务必确认 `.env` 未被跟踪（当前根 .gitignore 已防护），并轮换智谱 API Key
3. **测试进程**：验证用的 uvicorn（8000）与 Celery worker 仍在后台运行，长期不用可停掉；下次正常启动脚本会自动接管
4. **后续方向**：解析任务对账（pending/indexing 卡死恢复）、最小自动化测试集（本次验证脚本可沉淀为 pytest 回归用例）

---

## 六、验证环境说明

- 验证方式：真实服务链路（Docker PG/Redis + uvicorn + Celery worker + NestJS server）+ curl 构造请求矩阵
- 覆盖：鉴权正反例、上传白名单正反例、CORS 预检、越权操作矩阵（403/404/200）、private 隔离切换、路径穿越注入、弱密钥启动拒绝
- 全部测试数据（恶意文档、临时文件）已在验证后清理
